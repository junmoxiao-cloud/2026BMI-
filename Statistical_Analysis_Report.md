# 脑电图时频消融统计学分析说明

## 1. 统计检验方法与目的
为评估在时频掩码（STFT Temporal Ablation）实验中，各特定时间窗与频段（Time Window × Frequency Band）被遮蔽后对模型分类性能下降的影响是否具有统计学显著性，我们采用了 **McNemar 检验 (McNemar's Test)**，并结合 **FDR (False Discovery Rate) 多重比较校正** 计算最终的 q-value。

- **McNemar 检验**：适用于配对名义数据的差异显著性检验。在本研究中，用于对比 Baseline 模型（未掩码）与不同条件模型（特定掩码）在 200 张图像预测结果（正确/错误）上的转变（从对变错 vs 从错变对）。
- **FDR 校正**：采用 Benjamini-Hochberg (BH) 过程控制多重假设检验导致的假阳性率。共对 35 个单频段-时间窗掩码条件及数个对照条件的 p-value 进行校正，设定显著性阈值为 $q < 0.05$。

## 2. 统计检验结果摘要

根据 McNemar 检验及 FDR 校正后的结果，仅以下核心时频区域的掩码导致了具有统计学显著性 ($q < 0.05$) 的性能下降：

| 实验条件 | Top-1 准确率下降 | 原始 p-value | FDR q-value | 显著性 ($q < 0.05$) | 神经生理学对应意义 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **full_mask_all** | 69.0% | $5.74 \times 10^{-42}$ | $2.18 \times 10^{-40}$ | **显著** | 全时频信息掩码（基准参考） |
| **T1_50-150ms × Alpha** | 18.5% | $5.73 \times 10^{-8}$ | $1.09 \times 10^{-6}$ | **显著** | V1 视觉皮层早期响应及 Alpha 抑制 |
| **T2_150-300ms × Beta** | 11.0% | $1.13 \times 10^{-4}$ | $1.43 \times 10^{-3}$ | **显著** | N170 面孔/物体高级语义处理与反馈调节 |

**注：** 
虽然部分条件如 **T1_50-150ms × Beta** (Drop: 5.0%) 在未校正前具有边缘显著性 ($p = 0.031$)，但在 FDR 多重比较校正后 ($q = 0.293$)，不满足统计学显著标准。其余时频掩码条件均未观察到显著的性能下降。

## 3. 多重比较校正策略：离散 vs 连续条件

针对不同类型的实验条件与可视化图表（离散条件的热力图/柱状图 vs 连续条件的折线图），我们在统计推断中采用了不同的 p-value 校正策略，以平衡假阳性率（Type I error）与统计功效（Statistical power）：

### 3.1 离散独立条件（Heatmap / Bar charts）—— 采用 FDR 校正 (q-value)
在 Phase 1 (STFT 时频掩码) 和 Phase 3 (全频段掩码) 实验中，我们对整个时频网格（如 7个频段 × 5个时间窗 = 35种条件）进行了全面、无先验假设的探索性分析。
- **原因**：这类实验属于典型的“多重假设检验”问题。由于各个时频块的掩码彼此独立，为避免在大规模比较中随机产生假阳性结果，必须进行严格的校正。
- **标准**：采用 Benjamini-Hochberg (BH) 过程控制 False Discovery Rate (FDR)，以校正后的 **q-value < 0.05** 作为最终显著性判定标准。

### 3.2 连续/序列条件（Line charts）—— 采用原始 McNemar 检验 (raw p-value)
在 Phase 2 实验中，我们针对已在 Phase 1 中确定的关键时频组合，施加了参数连续变化的扰动（如振幅缩放比例、相位随机化程度、信噪比变化）。
- **原因**：这些扰动参数构成了具有明确先验物理意义的连续序列。序列中相邻数据点之间存在极强的内在相关性与逻辑依赖，且实验旨在验证特定参数递变趋势（如随扰动加深性能如何非线性变化），而非毫无关联的独立搜索。如果在此类高度相关的连续参数测试中强行应用 FDR 校正，将导致校正过度保守（Over-correction），极大损害统计功效，掩盖真实的物理趋势。
- **标准**：对于这类具有强序列依赖性的参数扰动折线图（Line charts），我们依据统计学惯例，直接采用原始的 **McNemar 检验 p-value < 0.05** 来判定特定参数变化节点与 Baseline 之间的差异显著性，不再进行 FDR 多重比较校正。

## 4. 图表显著性视觉区分标准

为了在各类可视化图表（如热力图 Heatmap、柱状图 Bar charts、折线图 Line charts 等）中直观且准确地传达统计检验结果，本研究采用了统一的双重显著性视觉区分标准进行标注：

- **单星号 (`*`)**：代表**原始显著但未通过校正**。即该条件下的原始 $p < 0.05$，但在经过 FDR (False Discovery Rate) 多重比较校正后，其 $q \ge 0.05$。这提示该效应具有一定的趋势或参考价值，但在严谨的多重检验标准下不够稳健。
- **双星号 (`**`)**：代表**严格显著且通过校正**。即不仅原始 $p < 0.05$，且经过 FDR 校正后的 $q < 0.05$。这表明该条件下的性能差异在统计学上是高度可靠且具有决定性意义的。

## 4. 结论
通过严谨的配对统计学检验，本研究确认了 **T1 阶段的 Alpha 频段** 和 **T2 阶段的 Beta 频段** 是支持模型进行脑电-图像跨模态检索的**核心且不可替代**的信息载体。消除这两部分特征会引起显著的预测错误增加，该结果为模型提取的特征在认知神经科学层面（如早期视觉感知及高级语义反馈调节）提供了坚实的统计学支撑。
