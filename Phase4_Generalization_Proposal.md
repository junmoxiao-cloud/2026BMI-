# Phase 4 Generalization Proposal: 跨维度特征依赖度分析 (Experiment B 深度拓展)

## 1. 核心目标
本提案旨在剥离其他不相关的泛化实验，**完全聚焦于实验 B**：探究并量化模型在频域 (Spectral)、空域 (Spatial) 和时域 (Temporal) 三个维度的特征依赖度。
通过统一的散点图可视化设计，我们将直观地展示在不同维度的信息被系统性扰动/破坏时，模型性能的衰减趋势，从而实现对模型跨维度依赖能力的横向对标与综合评估。

---

## 2. 可视化设计总体原则与统一逻辑
为了确保 Spectral, Spatial, Temporal 三类散点图具有统一的可比性，支撑跨维度评估，所有图表必须遵循以下一致的设计规范：

- **统一的 X 轴 (自变量)：扰动比例 / 信息破坏率 (Perturbation / Masking Ratio)**
  - 物理意义：衡量对应维度输入特征被掩盖、打乱或破坏的比例。
  - 刻度规范：`[0.0, 1.0]`，步长 `0.1`。
  - 逻辑对齐：`0.0` 代表完整保留该维度特征（无扰动，Baseline）；`1.0` 代表该维度特征被完全破坏（完全随机化或全掩码）。

- **统一的 Y 轴 (因变量)：相对性能保持率 (Relative Performance Retention)**
  - 物理意义：模型在受到扰动后的表现，相对于基线性能的比例，用于消除不同模型绝对性能差异带来的干扰。
  - 计算公式：$Y = \frac{\text{Accuracy}_{\text{perturbed}} - \text{Accuracy}_{\text{random}}}{\text{Accuracy}_{\text{baseline}} - \text{Accuracy}_{\text{random}}}$ (若二分类，随机准度为0.5) 或简单的 $\frac{\text{Accuracy}_{\text{perturbed}}}{\text{Accuracy}_{\text{baseline}}}$。
  - 刻度规范：`[0.0, 1.0]`，步长 `0.1`（或 `0.2`）。`1.0` 对应基线性能无损，`0.0` 对应性能降至随机猜测水平。

- **散点图表现形式与度量**
  - 使用散点图并拟合出性能衰减曲线（如三次样条拟合或指数衰减拟合）。
  - **核心指标 (依赖度量化)**：曲线的下降斜率（或曲线下面积 AUC）。曲线下降越陡峭、AUC 越小，说明模型对该维度特征的**依赖度越高**；曲线越平缓，说明模型具有较强的抗干扰能力与冗余特征利用率。

---

## 3. 三维度依赖度散点图具体方案

### 3.1 频域 (Spectral) 依赖度散点图
通过屏蔽 EEG 信号的特定频段，观察模型对频率信息的依赖。

* **X 轴变量定义**：频段掩码比例 (Spectral Band Masking Ratio)。
  * *操作方式*：对输入信号进行带阻滤波或傅里叶变换后频域掩码。从0开始，逐渐增加屏蔽的频率带宽比例（例如随机屏蔽某些频点，或者从高频/低频向中心频段逐步屏蔽）。
* **Y 轴变量定义**：相对分类准确率保持率。
* **预期结论提取**：
  * 若随 X 增大，Y 值迅速崩塌，说明模型高度依赖频域能量分布特征（可能是特定任务如运动想象的 alpha/beta 节律）。
  * 若 Y 值下降缓慢，说明模型在频域之外（如时域动态）找到了丰富的判别特征。

### 3.2 空域 (Spatial) 依赖度散点图
通过打乱或屏蔽不同比例的电极通道，评估模型对头皮空间拓扑结构和全局空间特征的依赖。

* **X 轴变量定义**：电极通道扰动比例 (Spatial Channel Masking/Shuffling Ratio)。
  * *操作方式*：随机将占总数 $X\%$ 的电极通道置零（Masking）或者相互随机置换位置（Shuffling），破坏空间拓扑关系。
* **Y 轴变量定义**：相对分类准确率保持率。
* **预期结论提取**：
  * 陡峭的衰减曲线：揭示模型高度依赖特定的通道组合（如 CSP 提取的共空间模式）。一旦局部电极损坏或位置偏差，模型将严重失效，提示空间泛化能力较差。
  * 平缓的衰减曲线：说明模型具有空间冗余度，提取了分布式的空间特征，对部分电极脱落具有强鲁棒性。

### 3.3 时域 (Temporal) 依赖度散点图
通过打乱时间步或添加时域掩码，评估模型是否依赖于连续的序列演变模式。

* **X 轴变量定义**：时序块掩码/置换比例 (Temporal Block Masking/Shuffling Ratio)。
  * *操作方式*：将时序序列切分为多个等长的时间块 (Blocks)。随机将 $X\%$ 的时间块置零，或者打乱这些时间块的时序排列（破坏长程依赖）。
* **Y 轴变量定义**：相对分类准确率保持率。
* **预期结论提取**：
  * 若打乱时序后性能显著下降（陡峭曲线）：表明模型深度依赖于脑电信号的动态演变和时间因果关系（如 RNN/Transformer 捕获的时序特征）。
  * 若性能几乎不下降（水平曲线）：说明模型主要充当“空间特征提取器”或“频域能量统计器”，忽略了时间维度的演变信息，仅对局部瞬态特征敏感。

---

## 4. 跨维度对比与综合结论
1. **多维度散点图叠加/并排对比**：将 Spectral, Spatial, Temporal 三条衰减曲线绘制在同一张图表内（或三个同坐标轴的子图中），使用不同颜色和标记。
2. **综合依赖度雷达图 (可选)**：根据散点图计算出的三个维度的性能衰减速率（依赖度分值），绘制雷达图。
3. **最终结论输出**：
   * 诊断目标模型是 **“偏科”模型**（例如高度依赖空域而忽略时域）还是 **“全能”模型**（在三个维度都能提取并利用冗余特征）。
   * 指导后续模型改进策略：对于过度依赖某一维度的模型，可在训练阶段引入针对性的数据增强（如 Spatial Dropout, Spectral Jittering），以强制模型学习其他维度的泛化特征。
