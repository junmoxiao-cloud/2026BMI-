# Accuracy Drop 指标定义与计算说明

## 1. 指标简介
Accuracy Drop（准确率下降）指标用于量化在消融实验中，去除（或掩码）特定特征后，模型性能相较于基线（Baseline）模型的下降程度。该指标是评估特征重要性和模型对特定特征依赖性的核心标准。

## 2. 计算逻辑与公式推导
Drop 指标的计算逻辑基于严格的对比分析，通过计算基线模型准确率与消融特征后模型准确率的差值来获得。

**核心公式：**
$$ \text{Drop} = \text{Accuracy}_{\text{Baseline}} - \text{Accuracy}_{\text{Ablated}} $$

- **$\text{Accuracy}_{\text{Baseline}}$**：基线模型（即未对特征进行任何消融、掩码或干预的原始模型）在验证集或测试集上的准确率。
- **$\text{Accuracy}_{\text{Ablated}}$**：消融模型（即去除了特定特征输入，或对特定特征进行掩码处理的模型）在相同验证集或测试集上的准确率。

通过该差值，可以直接量化被去除特征对模型整体预测能力的贡献度。

## 3. 取值范围与物理及模型意义
Drop 指标的理论取值范围为 $[-1, 1]$（若准确率以小数表示）或 $[-100\%, 100\%]$（若以百分比表示）。不同的取值反映了不同的模型特征依赖关系：

- **正值 ($\text{Drop} > 0$)**：
  - **模型意义**：消融后模型性能下降，说明被去除的特征对模型做出正确预测是有益的。
  - **物理意义**：表示模型高度依赖该特征进行决策，该特征包含关键的判别信息（**特征依赖**）。正值越大，说明模型对该特征的依赖性越强，特征越重要。

- **零值 ($\text{Drop} \approx 0$)**：
  - **模型意义**：消融前后模型性能无显著变化。
  - **物理意义**：表示被去除的特征对当前模型的预测几乎没有影响。这可能是因为该特征本身是冗余的，或者模型能够从其他存在的特征中获取与之相同的信息。

- **负值 ($\text{Drop} < 0$)**：
  - **模型意义**：消融后模型性能反而提升，即 $\text{Accuracy}_{\text{Ablated}} > \text{Accuracy}_{\text{Baseline}}$。
  - **物理意义**：表示被去除的特征对模型而言是干扰项或噪声。去除该特征不仅没有损害模型的表达能力，反而消除了底噪干扰，为模型带来了性能上的提升（**去除干扰底噪增益**）。

## 4. 计算涉及的核心参数与数据获取要求

为了确保 Drop 指标计算的科学性与可复现性，需严格控制以下核心参数与评估设置：

### 4.1 基线（Baseline）设置
- **模型状态**：必须使用训练完全收敛且未经过额外消融微调的原始预训练模型权重。
- **输入特征**：包含所有完整模态/维度的特征输入，不施加任何人为的掩码或噪声干扰。

### 4.2 评估指标（Top-1 评估设置）
- 通常采用 **Top-1 Accuracy** 作为评估标准。即模型预测概率最高的类别与真实标签匹配的样本数占总样本数的比例。
- **一致性要求**：确保 Baseline 和 Ablated 模型在评估时采用完全相同的 Top-1 计算逻辑、分类阈值（若有）以及评估代码路径。

### 4.3 数据获取与预处理要求
- **数据集对齐**：Baseline 和 Ablated 模型的评估必须在完全相同且未经打乱（或严格采用相同随机种子）的验证集或测试集上进行，以排除数据分布差异带来的影响。
- **消融策略的严谨性**：特征去除必须彻底且一致。如果是采用掩码策略，需明确是置零（Zero-masking）、均值填充（Mean-padding）还是引入随机噪声，且整个消融过程需在数据加载阶段或模型输入前严格统一执行。
- **环境与超参数**：在显存允许的情况下，保持测试时的 Batch Size 一致，并确保模型处于 `eval()` 模式，关闭 Dropout，固定 BatchNorm 的统计量，避免推理时的随机性干扰。